void insert_sort(int num, int* list. int* result)
{
	int key;
	int exchange;
	*result = *list
	for(int i = 1; i < num; i++)
	{	

		key = *(list+i)
		int j = i-1;
		while( (key < *(result + j) ) && (j > 0))
			
			j--;
	}
}